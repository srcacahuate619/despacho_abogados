# URL del backend (túnel Cloudflare, ngrok, o IP del servidor)
# Dejar vacío si el backend está en el mismo origen via rewrites
NEXT_PUBLIC_API_URL=https://prefer-sealed-refined-listings.trycloudflare.com
